# Tracebase Notices

Tracebase is licensed under the MIT License. See `LICENSE`.

The npm package depends on third-party open source packages. The current runtime dependency is:

- `jszip`, licensed under MIT or GPL-3.0-or-later. Tracebase uses it under the MIT option.

Development and build dependencies are installed only for source checkout workflows such as testing and dashboard builds. Their license metadata is recorded in `package-lock.json`.
